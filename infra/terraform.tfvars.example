# Copy to terraform.tfvars and fill in values.
# NEVER commit terraform.tfvars — it contains secrets.
#
# `do_token` is intentionally omitted from this example so CI can supply it
# through `TF_VAR_do_token` without being overridden by a checked-in file.
